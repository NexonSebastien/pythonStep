from serial import *
from threading import Timer
import time

"""ser = Serial("COM3",timeout=1)
print(ser)

while 1:
    donnee = ser.readline().decode('utf-8')
    #donnee = float(donnee)
    #ser.close()
    if (len(donnee))>0:
        if donnee[0].isdigit():
            print(donnee)"""
